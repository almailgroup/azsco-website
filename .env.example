# Copy to your host's environment settings — never commit a filled-in version.
# The website itself never sees these; only the proxy in api/ does.

MISTRAL_API_KEY=your-key-from-console.mistral.ai
MISTRAL_MODEL=mistral-small-latest
ALLOWED_ORIGIN=https://www.azsco.com
