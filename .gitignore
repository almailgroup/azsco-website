# OS / editor noise
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Dependencies / build output (if tooling is added later)
node_modules/
dist/
.cache/

# Secrets — the Mistral key belongs in the host's environment, never in the repo
.env
.env.*
!.env.example
.vercel
.wrangler
